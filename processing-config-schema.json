{
  "type": "object",
  "layout": "tabs",
  "required": [
    "datasetMode"
  ],
  "allOf": [
    {
      "title": "Liste de jeux de données",
      "default": {
        "datasetMode": "create",
        "dataset": {
          "id": "",
          "title": ""
        }
      },
      "oneOf": [
        {
          "title": "Créer des jeux de données éditables",
          "required": [
            "dataset"
          ],
          "properties": {
            "datasetMode": {
              "type": "string",
              "const": "create",
              "title": "Action"
            },
            "dataset": {
              "type": "object",
              "required": [
                "prefix"
              ],
              "properties": {
                "prefix": {
                  "type": "string",
                  "title": "Préfixe des noms des jeux de données",
                  "description": "Les titres des jeux de données sont construits avec ce préfixe et le nom de la couhe correspondante."
                }
              }
            }
          }
        }
      ]
    },
    {
      "title": "Paramètres",
      "required": [
        "url"
      ],
      "properties": {
        "url": {
          "title": "URL source",
          "type": "string",
          "description": "Les protocoles supportés sont HTTP et HTTPS, les formats supportés sont .zip et .gpkg."
        },
        "idsLayers": {
          "type": "array",
          "title": "Couches à extraire",
          "items": {
            "type": "integer",
            "minimum": 1
          },
          "description": "Les nombres à indiquer correspondent aux numéros des couches à extraire. Ne rien indiquer affichera seulement les couches disponibles sans les extraire."
        }
      }
    }
  ]
}